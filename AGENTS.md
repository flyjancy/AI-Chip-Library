# AI Chip Library Agent Guide

## 项目目标

- 这是一个以 AI Chip 为核心、可扩展到其他技术主题的个人资料库。
- 仓库保存原始资料、中文深度分析和根目录统一索引。
- `README.md` 是资料目录与分类规则的唯一事实来源；不要另建重复索引。
- 保持方案简单：优先 Markdown、目录和 Git，不增加数据库、生成脚本或依赖，除非现有规模已证明需要。

## 目录与分类

- 一级目录表示技术领域，二级目录表示该领域内的模块、技术或设计任务。
- `Architecture` 按硬件模块分类，例如 `GPU`、`NPU`、`NoC`、`DMA`。
- `Algorithm` 按算法技术分类，例如 `Quantization`、`Sparsity`、`Training`。
- `AI-Aided-Design` 按设计任务分类，例如 `RTL-Generation`、`RTL-Optimization`、`Verification`、`Physical-Design`。
- 只在有实际资料时创建二级目录，不预建空目录。
- 每份资料只保存一份并选择一个主分类；跨主题信息写入 README 的“相关主题”。
- 遇到跨一级领域且归属不明确的资料，先列出候选目录和理由，请示维护者；未经确认不要移动文件。

## 新资料收录流程

1. 读取文件元数据与必要正文，确认正式标题、年份、类型和原始来源。
2. 检查仓库内是否已有相同标题、DOI、arXiv ID 或等价文件，避免重复收录。
3. 提出一级目录、二级目录和文件名建议，等待维护者确认。
4. 使用正文正式完整标题生成文件名：保留原始大小写和数字，将连续空白与标点替换为单个 `-`，保留原扩展名。
5. 移动或重命名资料时，在同一次修改中更新 README 的本地文件、Summary、来源和相关主题链接。
6. 确认旧路径没有残留，所有本地链接均存在，再提交变更。

## 论文阅读流程

- 用户要求读取未阅读论文时，使用 `read-paper` skill，按 README 顺序逐篇处理，不自动生成横向对比报告。
- 开始前查找同目录下与正式标题对应的 `summary-<正式标题>.md`；已存在则跳过，绝不覆盖或添加序号。
- PDF 不超过 15 页时全文读取；超过 15 页时先扫描结构，再按每批最多 20 页覆盖问题、方法、实验、局限和结论。
- 文本抽取只用于定位内容。必须渲染并检查全部相关页面，核对架构图、数据流、表格、坐标轴、图例、脚注和样本量。
- 双栏 PDF 的抽取结果可能乱序或截断；必要时按单页重新提取，不能根据缺失片段猜测。
- 报告使用中文和 `read-paper` 深度分析模板，必须包含基本信息、一句话总结、动机、方法、实验、消融、局限、个人点评和“工程化三问”。
- 关键数字注明页码、表号或图号。明确区分论文证据、个人分析和工程推断；论文未覆盖的芯片架构、RTL 或验证内容标记为 `TBD` 或“推测”。
- 对 placeholder、跨平台基线、仅综合结果、估算值和不完整统计披露必须明确降级其证据强度，不把它们写成确定事实。
- 完整报告生成后，再同时检查目标 PDF 与 Summary 路径；任一路径已存在就停止该篇，不覆盖。
- 成功后按正式标题重命名 PDF、写入同目录 Summary，并立即更新 README 的两个链接。
- 临时文本和页面渲染放在临时目录，完成后清理，不提交到仓库。

## README 维护

- 索引字段保持为：标题、年份、类型、分类、本地文件、Summary、原始来源、相关主题。
- 新增、删除、移动、重命名或完成阅读时都必须同步索引。
- 目录结构代码块使用标准 Unicode 树形字符 `├──`、`│`、`└──`；不要使用 `|--` 和反引号模拟连接线。
- 一个资料文件在索引中恰好出现一次；一个 Summary 必须对应同目录的一份原文。
- 不在分目录创建 README，除非根索引已经明显难以维护。

## 验证清单

- 运行 `git diff --check`，确保没有空白错误。
- 检查 README 中每个本地 PDF 和 Summary 链接都指向现有文件。
- 检查全部 PDF 都被索引，全部 `summary-*.md` 都有对应 PDF，旧文件名不再被引用。
- 对论文报告确认模板章节完整，工程化三问顺序为“瓶颈 -> 结构或训练方法 -> 芯片架构/RTL/验证”。
- 重命名后使用 `pdfinfo` 确认文件仍可读取、未加密且页数不变。
- 提交前检查 `git status`，不混入临时文件、`.DS_Store` 或无关改动。

## Git 与发布

- GitHub 仓库为 private `flyjancy/AI-Chip-Library`，默认分支为 `main`，远端名为 `origin`。
- 当前 PDF 体量较小，直接由 Git 管理；只有仓库大小实际造成克隆或推送问题时再考虑 Git LFS。
- 已确认的资料整理或文档修复完成并通过检查后，创建内容明确的普通提交并推送 `main`。
- 禁止 force push、覆盖已有 Summary、重写历史或删除原始资料，除非维护者明确要求。
- `.DS_Store` 必须保持在 `.gitignore` 中。

## 已验证的经验

- 文件名规则必须与阅读 skill 一致；短标题和正式标题并存会导致重复重命名与 README 断链。
- PDF 改名、Summary 创建和 README 更新应视为一个原子变更。
- 仅靠 `pdftotext` 会漏掉或打乱图表信息，深读必须结合逐页视觉检查。
- 论文声称的硬件收益要区分算法代理指标、post-synthesis、post-layout 和 silicon measurement。
- 简单的目录与单一索引目前足够；不要提前引入自动索引、空分类或额外元数据系统。
